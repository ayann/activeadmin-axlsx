require 'spec_helper'

describe Activeadmin::Axlsx do
  it 'has a version number' do
    expect(Activeadmin::Axlsx::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
